# decisions 目录说明

本目录记录每次 AI 使用后的最终决策，并要求后续复盘。

## 触发条件
- 每次使用 AI 后，必须新建或更新一条决策记录。

## 使用规则
- 若未产出或更新 decision，则该次 AI 使用视为无效。
- 编号规则：从 `001` 开始递增。
- 每条 decision 在 90 天后必须复盘；不复盘不计入有效决策。
- 使用 `TEMPLATE.md` 统一结构，确保可追踪、可复盘、可纠错。
